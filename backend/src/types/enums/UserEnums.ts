export enum SkillCategory {
    EXPERTISE,
    ROLES,
    METHODS_AND_TOOLS
}

export enum AccountType {
    CLIENT = "CLIENT",
    CONSULTANT = "CONSULTANT"
}

export enum ProfileLevel {
    ENTRY_LEVEL,
    JUNIOR,
    MIDDLE,
    SENIOR,
    EXPERT,
}
